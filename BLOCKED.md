BLOCKED: The full suite has seven failures in `tests/test_measure.py`: serialized observations now contain `timing_channel`, but the loader reconstructs Observation values without it, so exact record round trips differ. The requested file allowlist excludes `ohyesmlx/measure.py` and `tests/test_measure.py`, which are needed to resolve the mismatch while retaining the new channel field. I need permission to expand the allowlist or a decision that the record shape may change. The baseline suite was 497 passing; this run is 511 passed, 7 failed. Directly affected tests pass (134 passed in `tests/test_runtimes.py` and `tests/test_sample.py`); stale-app path matching was also corrected and its regression tests pass.
