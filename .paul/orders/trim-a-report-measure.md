GOAL: Remove duplicate and dead code in report.py and measure.py. Rendered output must stay byte-identical except where item (e) says otherwise.
FILES: ohyesmlx/report.py, ohyesmlx/measure.py, tests/test_report.py, tests/test_measure.py. Touch nothing else (not docs/, not scripts/).
ITEMS:
(a) One formula definition. report._per_request must call measure.decode_tps / prefill_tps / itl_s and apply only report's MIN_CONTENT_DELTAS gate on top. Where the two definitions disagree (0 completion tokens, 0 prompt tokens, negative span) keep measure's; list every test expectation that changed and why.
(b) Delete report.dir_bytes and its tests; _row uses result.disk_bytes only (measure.artifact_bytes is the one disk-size definition).
(c) Merge the grid/sweep join duplicates in report.py: _check_cells_appear_once + _check_sweep_cells_appear_once -> one guard taking a key function; _check_one_artifact_per_label + _check_one_version_per_runtime -> one "one value per key" helper (error messages unchanged); the shared pins/workloads lines of _provenance/_sweep_provenance -> one helper; _grid_table/_sweep_table share one row builder; _format_labels/_sweep_cells/_columns ordered-unique loops -> dict.fromkeys. Also the duplicated "| entry | means |" legend -> one constant.
(d) measure.py: delete the try/except ImportError blocks, _require_modules and MeasureError (import token_counter, runtimes, CACHE_STATES, transport directly); inline _observation_record as asdict. Delete tests that exist only for MeasureError.
(e) Remove the "fits" floor: FLOORS = ("coherence", "metrics"); delete FITS_DETAIL, FLOOR_STATES and "not evaluated" from FLOOR_CLEARED; update _floor_verdicts, and the footnote sentence about fits. Rendered cards/footnotes lose their fits lines; update those test expectations and nothing else.
CONSTRAINTS: Do not delete or reword any docstring/comment you are not removing code for. Keep the drift, deferred-load, short-window and cross-runtime rules exactly. No new dependency.
ACCEPTANCE: `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` green (baseline 504; report the new count and every deleted test by name). `git diff --stat` touches only the four files above. Report net line change per file.
