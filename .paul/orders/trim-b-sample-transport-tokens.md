GOAL: Remove dead code in sample.py, transport.py and token_counter.py without changing any measured value.
FILES: ohyesmlx/sample.py, ohyesmlx/transport.py, ohyesmlx/token_counter.py, tests/test_sample.py, tests/test_transport.py, tests/test_token_counter.py, tests/test_measure.py (only its fake sampler's "power" key). Touch nothing else.
ITEMS:
(a) sample.py: delete power_sample, passwordless_sudo, DEFAULT_SAMPLERS and Sampler's samplers/power parameters; Sampler.stop() no longer writes a "power" key. This host has no passwordless sudo, so it has never produced data. Delete their tests; drop the "power" key from test_measure.py's fake sampler.
(b) transport.py: the non-HTTPResponse stream path is unreachable (connection.getresponse always returns http.client.HTTPResponse; coverage shows the peek branch and the `response.fp` fallback never run). Remove the peek/settimeout(0) branch and the fallback, keeping read1 on the HTTPResponse, the select() wait, and every comment that explains a kept line. Replace the three identical usage int-validation blocks with one helper; error messages unchanged.
(c) token_counter.py: transport always passes usage_reasoning_tokens=None, so delete that parameter and its branch from resolve_token_accounting (update the call in transport.py and the tests). Move FixedMapTokenCounter into tests/test_token_counter.py.
CONSTRAINTS: The SSE loop's behaviour on real HTTPResponse streams must not change: chunked decoding, timeout handling, [DONE], mirrored/reasoning-only channel selection. If a test relies on a non-HTTPResponse fake, report BLOCKED with the test name rather than deleting it.
ACCEPTANCE: `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` green (baseline 504; report new count and every deleted test by name). `git diff --stat` touches only the files above. Report net line change per file.
