GOAL: Hardening Phase 2 (review items A5, A7, D1, D3 in .paul/review/2026-09-23/SUMMARY.md; decisions 119 and 120 in .paul/STATE.md). Phase 1 already landed: summarize() rows keep every computed value and rendering suppresses measured figures on non-PASS rows ("not published: <status> row"); `transport.timing_channel(observation)` returns "reasoning" or "content".
FILES: ohyesmlx/report.py, ohyesmlx/measure.py (only if an item genuinely needs it), tests/test_report.py, tests/test_measure.py, docs/interfaces.md. Touch nothing else.
ITEMS:
(A5, Decision 119) Rows timed on the reasoning channel stay timed that way and are labelled. summarize() adds "reasoning_timed_note": None when every measured sample's timing_channel is "content", else "timed on reasoning channel: <k> of <n> measured requests (TTFT and decode are the reasoning stream's, not content's)". Render it wherever the other *_note fields render in the leaderboard table notes and the metric card, and in grid/sweep entries as a visible marker whose meaning is stated once under the table. One definition: use transport.timing_channel, do not re-derive.
(A7, Decision 120) Runtime-axis readings in render_grid (and any other renderer that orders one format across runtimes) print NO ordering when the rank metric is in CROSS_RUNTIME_UNCOMPARABLE: list each runtime's value unordered (alphabetical by runtime) with the existing blockquote caveat, and no "1.", "fastest", arrow, rank position or recommendation for that metric across runtimes. Format-axis (within-runtime) ordering is unchanged. If _recommendation would rank across runtimes on such a metric, it must not.
(D1) End-to-end latency P50/P90/P99 from each measured sample's total_s: row fields "e2e_p50_s", "e2e_p90_s", "e2e_p99_s", same MIN_PERCENTILE_N rule and percentile() as TTFT (no second percentile function). Add them to the leaderboard table and metric card beside TTFT, to RANK_METRICS as "lower", and to the set suppressed on non-PASS rows. Only samples that came back count; a failed request is not a latency.
(D3) render_grid and render_sweep refuse (ValueError naming the run dir, cell and the string) any row whose runtime_version starts with "unknown" — a version the runtime would not state cannot be held constant. Single-run leaderboards still render such rows unchanged. Put the check next to the existing runtime_version guard; one place, used by both joins.
docs/interfaces.md: add the four new row fields and the D3 refusal to the record/join description. Nothing else in it.
ACCEPTANCE: one red-checked test per item (revert, see it fail for the stated reason, restore; report each). Suite green: /Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q (baseline 530; report count). `git diff --stat` shows only the listed files. Do not commit.
