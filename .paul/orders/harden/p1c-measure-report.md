GOAL: Hardening Phase 1, wave 2 (review items A1, A2-domain, B3-handling, C1, C2, C4-tmp, D2, E1 in .paul/review/2026-09-23/SUMMARY.md). Wave 1 already landed: `transport.timing_channel(observation)` derives the timed stream from the two texts (no stored field), one-delta streams now have last_content_s == ttft_s, and runtimes raise RuntimeStopError when cleanup cannot be verified.
FILES: ohyesmlx/measure.py, ohyesmlx/report.py, tests/test_measure.py, tests/test_report.py. Touch nothing else.
ITEMS:
(A2) Move the content_event_count >= 2 domain into measure.decode_tps, prefill_tps and itl_s (None below it). Move MIN_CONTENT_DELTAS and its comment from report.py to measure.py; report uses measure.MIN_CONTENT_DELTAS (update tests that referenced report's). report._per_request then just calls the three. measured_drift and _warmup_rate therefore stop consuming one-delta rates.
(A1) A row whose status is not PASS publishes no measured figure in the leaderboard table or the metric card: ttft p50/p90/p99, itl, decode, drift, aggregate, prefill print as the same "—" with the note "not published: <status> row" (cold_load_s, first_request_s, peak_mb, disk_bytes and runtime_version stay: they are facts about the start, not the requests). The summarize() row dict keeps every computed value, so the record stays recomputable; only rendering suppresses.
(B3) In _visit, a RuntimeStopError (from start or from handle.stop) is not a "retry": persist results (write_jsonl) and re-raise, ending the run. A run that cannot confirm a runtime stopped must not start the next one.
(C1/C2) Wrap everything after runtime.start() returns in the try/finally that stops the handle. On any exception or KeyboardInterrupt escaping a visit, write_jsonl the results gathered so far before re-raising (run_cells: try/except BaseException around the visit, write, raise).
(C4) write_jsonl removes its .tmp file if writing fails, then re-raises; the previous results.jsonl is untouched.
(D2) Header field "harness": {"version": ohyesmlx.__version__, "source_sha256": sha256 over the bytes of ohyesmlx/*.py sorted by name}. Not a PIN_FIELDS entry. render_grid/render_sweep refuse when two runs BOTH carry "harness" and the source_sha256 differs (ValueError naming both run dirs); a run without it joins as before. Provenance prints it.
(E1) FakeRuntime/FakeHandle can carry an api_key; add a test that an oMLX-shaped handle's key reaches transport.chat.
ACCEPTANCE: one red-checked test per item (report each). Suite green (report count; baseline is wave 1's). `git diff --stat` only the four files.
