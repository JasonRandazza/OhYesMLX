GOAL: Deep review, area 2 of 6: scripts/ versus the harness. For every file in scripts/, give a table row with its purpose, which docs/research/*.md cite it (grep for its name), which ohyesmlx modules it imports, and which harness safeguards it bypasses: the coherence gate, one model resident with the port confirmed free, pinned temperature/seed/max_tokens, the Osaurus settings baseline, plateau warmup, raw-observation retention, and runtime version recording. Then findings for any published research result that rests on a script which bypassed a safeguard, and for logic copied from ohyesmlx/ instead of imported.
FILES TO READ: scripts/*, docs/research/*.md, ohyesmlx/*.py, AGENTS.md
OUTPUT: write exactly one file, .paul/review/2026-09-23/scripts.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
