GOAL: Deep review, area 6 of 6: documentation drift. Compare README.md, docs/interfaces.md, docs/runtimes/*.md, AGENTS.md and `ohyesmlx <cmd> --help` text against the code as it is now. Every claim about a function signature, field, flag, default, port, file path, record shape or behaviour that the code contradicts is a finding. Also flag references to deleted things (pareto, power sampling, fits floor, dir_bytes, MeasureError, PROMPT_TOKEN_TARGETS) and any rationale restated in more than one place with different content.
FILES TO READ: README.md, docs/interfaces.md, docs/runtimes/*.md, AGENTS.md, ohyesmlx/*.py
OUTPUT: write exactly one file, .paul/review/2026-09-23/docs.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
