GOAL: Deep review, area 3 of 6: metric correctness. Check that TTFT (first content token, reasoning excluded), ITL, decode/prefill/aggregate throughput, P50/P90/P99, drift, cold load, first_request_s, peak_mb and disk_bytes are computed exactly as docs/interfaces.md and AGENTS.md define them, including edge cases: mirrored reasoning, reasoning-only streams, single-delta streams, zero tokens, concurrency > 1 batch spans, and failed requests mixed with good ones. Find any path where a wrong number is published rather than refused.
FILES TO READ: docs/interfaces.md, AGENTS.md, ohyesmlx/transport.py, ohyesmlx/measure.py, ohyesmlx/report.py, ohyesmlx/token_counter.py, ohyesmlx/sample.py
OUTPUT: write exactly one file, .paul/review/2026-09-23/metrics.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
