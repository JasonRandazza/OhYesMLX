GOAL: Deep review, area 5 of 6: test quality. Find fakes in tests/ whose behaviour differs from the real thing they stand in for (FakeRuntime/FakeHandle/FakeObservation/SSE servers vs runtimes.py/transport.py), tests that only assert on prose wording rather than behaviour, tests that cannot fail (red-check suspicious ones mentally and say why), and critical branches in ohyesmlx/ with no test that would fail if they broke. You may run coverage: `uvx --with pytest --with tokenizers coverage run --data-file=/tmp/ohyesmlx-review.cov --source=ohyesmlx -m pytest -q` then `uvx coverage report --data-file=/tmp/ohyesmlx-review.cov -m`.
FILES TO READ: tests/*.py, ohyesmlx/*.py
OUTPUT: write exactly one file, .paul/review/2026-09-23/tests.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
