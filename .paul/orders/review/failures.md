GOAL: Deep review, area 4 of 6: failure paths. In runtimes.py, measure.py, transport.py and sample.py, find every path where an exception, timeout, KeyboardInterrupt, dead process or partial write leaves a runtime running, a port held, a second model resident, a thread alive, a temp catalog or scratch dir behind, or results.jsonl inconsistent or lost. Also find any path where a failure is swallowed and the run carries on as if it measured cleanly.
FILES TO READ: ohyesmlx/runtimes.py, ohyesmlx/measure.py, ohyesmlx/transport.py, ohyesmlx/sample.py, ohyesmlx/osaurus_settings.py, ohyesmlx/cli.py, AGENTS.md
OUTPUT: write exactly one file, .paul/review/2026-09-23/failures.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
