GOAL: Deep review, area 1 of 6: are the rules in AGENTS.md actually enforced? For every item under "Metric definitions", "Hard boundaries" and "Runtime hazards, already paid for", state where the code enforces it (file:line) and which test proves it (test name), or mark it UNENFORCED or UNTESTED. Put that as a table first, then findings for every UNENFORCED or UNTESTED rule that could corrupt a published number.
FILES TO READ: AGENTS.md, ohyesmlx/*.py, tests/*.py
OUTPUT: write exactly one file, .paul/review/2026-09-23/invariants.md, and touch nothing else (no code, no tests, no docs, no git). Read-only review otherwise; do not run any runtime, server or model.
FORMAT: a short scope line, then findings ranked most-severe first, at most 25. Each finding: `### F<n> [critical|high|medium|low] <one-line claim>`, then `Where:` file:line, `Evidence:` the exact quoted lines (verbatim, so it can be checked), `Failure:` the concrete scenario that goes wrong, `Fix:` the smallest fix. No finding without quoted evidence. Speculation goes in a final "Unverified suspicions" list, not in findings. "No findings" is a valid result; padding is not.
You may run: grep, reading files, and `/Users/jrazz/.claude/jobs/1704c764/tmp/verify-venv/bin/python -m pytest -q` (tests only).
